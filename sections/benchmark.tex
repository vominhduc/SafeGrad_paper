\section{The \dataname Benchmark}

\subsection{Overview and Design Philosophy}

\dataname is designed around a core hypothesis: safety evaluation in T2I systems should be severity-graded rather than threshold-based.
Rather than a large flat set of binary-labeled examples, \dataname provides \emph{relational} severity ladders: a single ladder of four prompts encodes more information about a model's safety boundary than four independent samples, exposing \emph{where} along the spectrum the model first fails and how sharply.
Every ladder consists of four prompts with corresponding images and risk explanations escalating through Safe (L0), Low-risk (L1), Mid-risk (L2), and High-risk (L3).
Per-rung risk explanations make the intermediate regime concrete: in the Violence ladder (\SuppFig{\ref{app:fig:sample_3}}), L1 is stylized fictional conflict while L2 escalates to realistic injury. All rungs follow the \SuppTab{\ref{app:tab:taxonomy}} anchors (\SuppSec{\ref{app:examples}}).

\subsection{Hierarchical Risk Taxonomy}

We define a hierarchical taxonomy covering 11 top-level risk categories (\Fig{\ref{fig:overview}}), each a distinct dimension of potential harm in T2I generation, synthesized from platform policies and existing literature~\citep{dalle3,midjourney_guideline,helff2024llavaguard,li2025t2isafety,zhang2025t2iriskyprompt,google,microsoft,metaAI}.
Each category defines four severity levels with human-authored escalation rules, reviewed against these sources with boundary cases adjudicated manually (\SuppSec{\ref{app:taxonomy}}, \SuppTab{\ref{app:tab:severity}}, \SuppTab{\ref{app:tab:taxonomy}}); the pipeline instantiates these rules rather than defining severity itself.

\subsection{Dataset Statistics}
\dataname is the strict post-filtering yield of the ASL pipeline: 1,026 ladders (4,104 prompts) spanning 11 categories, each rung holding a prompt, reference image, and risk explanation (82--99 ladders per category; \SuppTab{\ref{app:tab:stats}}).
Categories are not mutually exclusive at high severity: co-occurrence analysis (\SuppSec{\ref{app:comatrix}}) shows the largest overlap for Violence--Disturbing (18.9\%/21.4\%).
\Tab{\ref{tab:comparison}} compares \dataname against existing benchmarks; \dataname targets continuous failure slopes at the L1/L2 boundary rather than flat coverage.

\begin{table}[tb]
\centering
\caption{Comparison of \dataname with existing benchmarks.
\textbf{Grad.} is severity-graded levels (vs.\ binary safe/unsafe); \textbf{PPL} is mean GPT-2 perplexity over each benchmark's highest-severity prompts (its unsafe set; L3 for \dataname), lower meaning more fluent, so metaphorical high-risk prompts avoid explicit keywords and defeat perplexity-based anomaly detection simultaneously; \textbf{Reason.} is per-sample risk explanation annotation. \dataname is the only T2I generation benchmark with graded severity, low-perplexity adversarial prompts, and risk explanations.}
\label{tab:comparison}
\resizebox{0.44\textwidth}{!}{%
\begin{tabular}{lrrrccc}
\toprule
\textbf{Dataset} & \textbf{\# Samples} & \textbf{\# Categories} & \textbf{\# Levels} & \textbf{Grad.} & \textbf{PPL ($\downarrow$)} & \textbf{Reason} \\
\midrule
UnsafeDiffusion~\citep{qu2023unsafe} & 434 & 5 & 2 & \xmark & 2,511  & \xmark \\
I2P~\citep{schramowski2023safe}               & 4,703  & 7  & 2 & \xmark & 2,587 & \xmark \\
T2ISafety~\citep{li2025t2isafety}           & ~70K & 12  & 2 & \xmark & 2,613 &  \xmark \\
T2I-RiskyPrompt~\citep{zhang2025t2iriskyprompt} & 6,432  & 14 & 2 & \xmark & 86 &  \cmark \\
\midrule
\textbf{SafeGrad (Ours)}                     & \textbf{4,104} & \textbf{11} & \textbf{4} & \cmark & 33.92 & \cmark  \\
\bottomrule
\end{tabular}%
}
\end{table}
